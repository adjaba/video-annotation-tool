{
  "name": "video-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"cd client && yarn start\" \"cd server && yarn start\"",
    "build": "cd client && yarn build && cd .. && env PORT=3000 NODE_ENV=production node server/src/index.js"
  },
  "dependencies": {
    "concurrently": "^4.1.0",
    "immutability-helper": "^3.0.1",
    "react-hotkeys": "^2.0.0",
    "semantic-ui-react": "^0.87.3",
    "video.js": "^7.6.0"
  }
}
